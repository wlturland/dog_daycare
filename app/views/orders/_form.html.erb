<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control", placeholder: "John Smith" %>
  </div>
  <div class="field form-group">
    <%= f.label :address %><br>
    <%= f.text_area :address, class: "form-control", placeholder: "123 TARDIS Way\nAll of Time and Space" %>
  </div>
  <div class="field form-group">
    <%= f.label :pay_type %><br>
    <%= f.select :pay_type, Order::PAYMENT_TYPES, {prompt: "Select a payment method"}, {class: "form-control"} %>
  </div>
  <div class="actions">
    <%= f.submit "Place Order", class: "btn btn-success" %>
  </div>
<% end %>
